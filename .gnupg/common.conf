# Shared GnuPG settings that are safe for a public dotfiles repository.
#
# Keep only non-sensitive options in this file.
# Do not put key material, keygrips, trust DB paths, or host-specific secrets here.
#
# Add machine-specific settings to an untracked local file in ~/.gnupg/.

