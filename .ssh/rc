#!/bin/bash
# Symlink forwarded SSH agent to a fixed path so zellij/tmux sessions survive reconnects.
if [ -S "$SSH_AUTH_SOCK" ] && [ "$SSH_AUTH_SOCK" != "$HOME/.ssh/agent.sock" ]; then
    ln -sf "$SSH_AUTH_SOCK" ~/.ssh/agent.sock
fi
